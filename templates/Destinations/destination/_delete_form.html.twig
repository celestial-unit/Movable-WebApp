<form method="post" action="{{ path('app_destination_delete', {'idDestination': destination.idDestination}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ destination.idDestination) }}">
    <button class="btn">Delete</button>
</form>
