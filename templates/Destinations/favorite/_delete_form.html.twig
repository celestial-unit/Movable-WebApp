<form method="post" action="{{ path('app_favorite_delete', {'id_favorite': favorite.id_favorite}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ favorite.id_favorite) }}">
    <button class="btn">Delete</button>
</form>
